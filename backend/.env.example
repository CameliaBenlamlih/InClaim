# Server Configuration
PORT=3001

# Flare Coston2 RPC
RPC_URL=https://coston2-api.flare.network/ext/C/rpc

# Relayer wallet private key (funds gas for submitting proofs)
RELAYER_PRIVATE_KEY=your_relayer_private_key_here

# Deployed contract addresses (update after deployment)
CONTRACT_ADDRESS=0x0000000000000000000000000000000000000000
FDC_VERIFIER_ADDRESS=0x0000000000000000000000000000000000000000

# ============================================================
# REAL DATA CONFIGURATION - AviationStack API
# ============================================================
# Get FREE API key at: https://aviationstack.com/signup/free
# Free plan: 100 requests/month, no credit card required
# Provides real-time flight status, delays, cancellations
AVIATIONSTACK_API_KEY=

# Optional: Test flight code for API testing (default: BA123)
TEST_FLIGHT_CODE=BA123

# ============================================================
# REAL FDC CONFIGURATION (FUTURE - after API whitelisting)
# ============================================================
# FDC Hub contract address (get from FlareContractRegistry)
# Leave empty to use hybrid mode (real data + MockFDCVerifier)
# Set this ONLY after AviationStack is whitelisted by Flare
FDC_HUB_ADDRESS=

# ============================================================
# MOCK FALLBACK CONFIGURATION
# ============================================================
# Used when AVIATIONSTACK_API_KEY is not set
# Options: ON_TIME, DELAYED, CANCELLED
MOCK_DEFAULT_STATUS=DELAYED
MOCK_DEFAULT_DELAY_MINUTES=90

# ============================================================
# EMAIL SMTP CONFIGURATION
# ============================================================
# SMTP server settings for sending confirmation emails
# For Gmail: use smtp.gmail.com, port 587, and App Password
# Get Gmail App Password: https://myaccount.google.com/apppasswords
SMTP_HOST=smtp.gmail.com
SMTP_PORT=587
SMTP_SECURE=false
SMTP_USER=your_email@gmail.com
SMTP_PASS=your_app_password_here

# Email sender details
FROM_EMAIL=noreply@inclaim.app
FROM_NAME=InClaim
